#!/usr/bin/env bash
# spark-code — owned Spark coding model CLI wrapper.
set -euo pipefail
ROOT="$(cd "$(dirname "$0")" && pwd)"
export PYTHONPATH="${ROOT}/python${PYTHONPATH:+:$PYTHONPATH}"
exec python3 "$ROOT/tools/spark-code/cli.py" "$@"
