# spark-voice-easy — owned STT/TTS heads

Train with:

```bash
make voice-easy # tiny dry (CI)
./spark-voice easy --scale large --device auto # opt-in large
```

Artifacts written here after train. Prefer RTX 5090; **never**
reserved voice GPUs. Measurement-only owned heads — not a vendor TTS clone.

See [docs/VOICE_EASY.md](../../docs/VOICE_EASY.md).
