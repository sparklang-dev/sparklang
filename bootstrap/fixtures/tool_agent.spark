# Tool + with tools — same shape as examples/tool_agent.spark
model code

tool weather(city: string) -> string {
  "stub:local"
}

with tools [weather] {
  ask "What's the weather hint for Springfield?" -> answer
}

print answer
