# Hook — tests/

Product: `spark-browser/tests/test_smoke.py`

Headless smoke: CA init path, HTTP/HTTPS MITM, HAR, filter/breakpoint
— **no GUI**. Run via `make test` in spark-browser (or
`make product-test` from this scaffold Makefile).
