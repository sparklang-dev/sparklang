# Spark IDE - proven bare ide key show (no prior open).
# After status-clear. Singular show alone validates default
# editor.ppm + keys_trace cmd show.
# Run: ./spark --dry-run examples/ide_key_show_alone.spark
# No Electron. No keymap n. No new asm - documents proven show only.

ide key show
