# Browser fixtures

| File | Purpose |
|------|---------|
| `demo_multiflow.har` | Dry/demo multi-flow HAR 1.2 (document + CSS + CDN JS + API + favicon). Copied by asm `mitm har export` under `--dry-run`. Mirrored in `spark-browser/fixtures/`. |
