# [goal] Live transfer / hold / hangup language ops — not shipped yet.
# Dry-runnable receptionist with expect gates:
# examples/receptionist.spark
# ./spark --dry-run examples/receptionist.spark
#
# This file stays the production-telephony goal marker.
# See docs/ADOPTION_BAR.md · docs/ROADMAP.md · docs/VOICE.md

model fast

classify Intent { support, sales, transfer, spam } from "I need to speak to a human about my order" min_confidence 0.7 -> intent

print intent

# [goal] transfer / hold / hangup / barge-in — language not yet.
# Dry path uses shell escape hatch in examples/receptionist.spark.
