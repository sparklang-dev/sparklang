# Escape hatch — allowlisted shell (dry = fixtures; live = execve).
# ./spark --dry-run examples/shell_escape.spark
# ./spark --live --allow-shell examples/shell_escape.spark

shell "echo hello" -> out
print out

shell "true" -> ok
print ok
