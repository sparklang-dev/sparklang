# Engine B — parse+css then layout→paint→show → out/engine/pipeline.ppm.
# Dry:  ./spark --dry-run examples/browser_engine_render.spark (no X11)
# Live: ./spark --live … → spark-engine-show on pipeline.ppm (needs DISPLAY)
# Fail closed without DOM (no paint_fixture fallback).

engine parse "engine/fixtures/style_basic.html" -> dom
engine css attach -> styles
browser engine render -> frame
