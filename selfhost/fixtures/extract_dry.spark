# Extract dry person stub — selfhost golden.
# Stage 1 lex subset; schema/from tokens parsed and discarded (vm.c).
# Avoid `let` (GAS scaffold pads dumps). Form from examples/extract_person.spark.
model fast

extract Person {
  name: string
  age: int
} from "Ada Lovelace was born in 1815" -> person

print person
