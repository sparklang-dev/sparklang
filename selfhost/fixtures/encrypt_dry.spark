# Encrypt gateway dry keywords lex fixture.
# Stage 1 golden (expected_encrypt_dry.tokens.jsonl).
# Keywords: crypto, encrypt, gateway (keygen/enable/on/off/seal/open/text stay IDENT).
# Forms from LANGUAGE.md / examples/encrypt_gateway.spark.
# Not a full compiler. Avoid `let` (GAS scaffold pads dumps).
model code

crypto keygen -> key
encrypt gateway enable key
gateway encrypt on
encrypt seal text "secret" -> blob
encrypt open blob -> plain
gateway encrypt off

print plain
